package manager
{
	import flash.events.EventDispatcher;
	import flash.events.IEventDispatcher;
	
	public class VolListPanelManager extends EventDispatcher
	{
		public function VolListPanelManager(target:IEventDispatcher=null)
		{
			super(target);
		}
	}
}