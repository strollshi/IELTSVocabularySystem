package manager
{
	import flash.events.EventDispatcher;
	import flash.events.IEventDispatcher;
	
	public class WordDetailManager extends EventDispatcher
	{
		public function WordDetailManager(target:IEventDispatcher=null)
		{
			super(target);
		}
	}
}