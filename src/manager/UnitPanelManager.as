package manager
{
	import flash.events.EventDispatcher;
	import flash.events.IEventDispatcher;
	
	public class UnitPanelManager extends EventDispatcher
	{
		public function UnitPanelManager(target:IEventDispatcher=null)
		{
			super(target);
		}
	}
}